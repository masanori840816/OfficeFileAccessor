<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      autoReload="true">

    <targets>
        <target xsi:type="File" name="accessFile"
            fileName="C:/tmp/logs/OfficeFileAccessor/access-${shortdate}.log"
            layout="${longdate} ${message}" />
        <target xsi:type="Console" name="outputconsole"
            layout="${longdate}|${event-properties:item=EventId_Id}|${uppercase:${level}}|${logger}|${message} ${exception:format=tostring}" />

        <target xsi:type="File" name="outputfile" fileName="C:/tmp/logs/OfficeFileAccessor/output.log"
            layout="${longdate}|${event-properties:item=EventId_Id}|${uppercase:${level}}|${logger}|${message} ${exception:format=tostring}|url: ${aspnet-request-url}|action: ${aspnet-mvc-action}" />
    </targets>

    <rules>
        <logger name="AccessLogger" minlevel="Info" writeTo="accessFile" final="true" />
        <logger name="*" minlevel="Warn" writeTo="outputconsole" />
        <logger name="*" minlevel="Warn" writeTo="outputfile" />
        <logger name="Microsoft.*" minLevel="Warn" final="true" />
    </rules>
</nlog>